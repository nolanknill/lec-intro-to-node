# Installation

1. Install packages
    - `npm install`


# Commands
1. Modify a string to upper or lwer case
    - `node app.js <string-to-modify> <lower-or-upper>`

2. Add a UUID to all objects without an ID:
    - `node add-ids-to-history.js`
